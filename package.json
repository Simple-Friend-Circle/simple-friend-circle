{
  "name": "friend-circle",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build index.ts --target=node --outfile=dist/index.js",
    "prepare": "husky",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.3",
    "prettier": "3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "rss-parser": "^3.13.0"
  }
}
