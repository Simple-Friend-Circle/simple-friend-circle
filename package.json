{
  "name": "friend-circle",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build index.ts --target=node --outfile=dist/index.js",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "rss-parser": "^3.13.0"
  }
}
